name: Build Image (branch:develop - linux/amd64)
on:
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build_image:
    uses: ./.github/workflows/build_image_for_arch.yml
    with:
      branch: develop
      platform: linux/amd64
      image_tag: develop
    secrets: inherit